[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: integration tests requiring full LangGraph event loop (deselected by default)
addopts = -m "not integration"
